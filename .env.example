# ArchitectAI environment

# At least one provider key is required
GOOGLE_GENERATIVE_AI_API_KEY=
GROQ_API_KEY=

# Optional: Sentry DSN for production error reporting (client loads separately if used)
# SENTRY_DSN=
